
env = Environment(
  CCFLAGS = ['-Wall', '-O3', '-pipe', '-funroll-loops', '-std=c++0x'],
  LIBS=['pthread'],
  LIBPATH=[],
  CPPDEFINES=[],
  LINKFLAGS=[],
  CPPPATH=['random']
)

env.Program( "main", ['main.cpp'] )
